# Website docs app (Fumadocs)

This folder contains the Fumadocs + Next.js website.
All markdown content lives at the repository root under `docs/`.

## Run locally

```bash
cd web
npm install
npm run dev
```

Then open http://localhost:3000/docs.

## Build

```bash
cd web
npm run build
npm run start
```
